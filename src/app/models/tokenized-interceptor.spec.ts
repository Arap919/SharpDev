import { TokenizedInterceptor } from './tokenized-interceptor';

describe('TokenizedInterceptor', () => {
  it('should create an instance', () => {
    expect(new TokenizedInterceptor()).toBeTruthy();
  });
});
